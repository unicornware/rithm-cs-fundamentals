/**
 * @file Searching Algorithms Exercise - linearSearchRecursive
 * @module csf/topics/searching/exercise/linearSearchRecursive
 */

import search from '@csf/01/02-recursion/exercise/search'

/**
 * Given an integer array, `array`, the function returns the index of `value`,
 * if found. If not found, the function will return `-1`.
 *
 * @example linearSearchRecursive([1, 2], 11) => -1
 * @example linearSearchRecursive([1, 3, 4], 2) => -1
 * @example linearSearchRecursive([1, 2, 3, 4], 0) => -1
 * @example linearSearchRecursive([1, 2], 1) => 0
 * @example linearSearchRecursive([1, 2, 3, 4], 4) => 3
 * @example linearSearchRecursive([1, 2, 3, 4, 5, 6, 7], 6) => 5
 *
 * @param {number[]} array - Integer array
 * @param {number} value - Number to search for in `array`
 * @return {number} Index of `target` if found, `-1` otherwise
 */
const linearSearchRecursive = search

export default linearSearchRecursive
